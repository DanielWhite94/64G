debug:
	cd client && make debug
	cd demogen && make debug
	cd editor && make debug
	cd mappng && make debug
	cd server && make debug
	cd slippymap && make debug

release:
	cd client && make release
	cd demogen && make release
	cd editor && make release
	cd mappng && make release
	cd server && make release
	cd slippymap && make release

clean:
	cd client && make clean
	cd demogen && make clean
	cd editor && make clean
	cd mappng && make clean
	cd server && make clean
	cd slippymap && make clean
